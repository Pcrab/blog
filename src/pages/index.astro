---
import Page from "@layouts/Page.astro";
import { getAllPosts } from "@utils/post/get";

const posts = getAllPosts().map((post, index) => {
    return {
        ...post,
        display: index.toString(),
    };
});
---

<Page current={1} posts={posts} baseUrl="/page/" />
